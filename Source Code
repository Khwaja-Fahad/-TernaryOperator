#include <stdio.h>

int main() {
    // Initialize variables
    int y;
    int x = 25;

    // Use ternary operator to assign value to y
    y = (x >= 6) ? 6 : x;

    // Display the value of y
    printf("y = %d\n", y);

    return 0;
}
